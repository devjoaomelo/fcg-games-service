apiVersion: apps/v1
kind: Deployment
metadata:
  name: fcg-games
  namespace: default
  labels:
    app: fcg-games
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fcg-games
  template:
    metadata:
      labels:
        app: fcg-games
        tier: backend
    spec:
      containers:
      - name: games-api
        image: 536765581095.dkr.ecr.us-east-2.amazonaws.com/fcg-games:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          protocol: TCP
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ConnectionStrings__GamesDb
          valueFrom:
            secretKeyRef:
              name: fcg-games-secrets
              key: connection-string
        - name: Jwt__Key
          valueFrom:
            secretKeyRef:
              name: fcg-shared-secrets
              key: jwt-key
        - name: Jwt__Issuer
          valueFrom:
            configMapKeyRef:
              name: fcg-games-config
              key: jwt-issuer
        - name: Jwt__Audience
          valueFrom:
            configMapKeyRef:
              name: fcg-games-config
              key: jwt-audience
        - name: Search__UseOpenSearch
          value: "false"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://127.0.0.1:4317"
        - name: OTEL_SERVICE_NAME
          value: "FCG.Games.Api"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      - name: adot-collector
        image: public.ecr.aws/aws-observability/aws-otel-collector:latest
        ports:
        - containerPort: 4317
          protocol: TCP
        env:
        - name: AWS_REGION
          value: "us-east-2"
        - name: AWS_OTEL_COLLECTOR_CONFIG_CONTENT
          value: |
            receivers:
              otlp:
                protocols:
                  grpc:
                    endpoint: 0.0.0.0:4317
            processors:
              batch: {}
            exporters:
              awsxray: {}
            service:
              pipelines:
                traces:
                  receivers: [otlp]
                  processors: [batch]
                  exporters: [awsxray]
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
