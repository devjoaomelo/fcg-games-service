apiVersion: v1
kind: Service
metadata:
  name: fcg-games-svc
  namespace: default
  labels:
    app: fcg-games
spec:
  type: LoadBalancer
  selector:
    app: fcg-games
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
