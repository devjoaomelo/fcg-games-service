apiVersion: v1
kind: Service
metadata:
  name: mysql-games
  namespace: fcg
spec:
  ports:
  - port: 3306
  clusterIP: None
  selector:
    app: mysql-games
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-games
  namespace: fcg
spec:
  serviceName: mysql-games
  replicas: 1
  selector:
    matchLabels:
      app: mysql-games
  template:
    metadata:
      labels:
        app: mysql-games
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: fcg-secrets
              key: mysql-root-password
        - name: MYSQL_DATABASE
          value: fcg_games
        - name: MYSQL_USER
          value: fcg
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: fcg-secrets
              key: mysql-password
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: gp2	
      resources:
        requests:
          storage: 5Gi